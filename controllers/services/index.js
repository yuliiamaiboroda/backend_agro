const { getAll } = require("./get-all");
const { create } = require("./create");
const { getCertain } = require("./getCertain");
const { remove } = require("./remove");
const { update } = require("./update");

module.exports = {
  getAll,
  create,
  getCertain,
  remove,
  update,
};